# ----------------------------------------------------------------------
# | SECURITY & CORE SETTINGS
# ----------------------------------------------------------------------
# Prevent directory listing
Options -Indexes

# Hide server version info
ServerSignature Off

# Disable ETags (Performance improvement for clusters, saves bandwidth)
FileETag None

# Block access to hidden files/directories (except .well-known)
<FilesMatch "(^#.*#|\.(bak|conf|dist|fla|in[ci]|log|psd|sh|sql|sw[op])|~)$">
    Require all denied
</FilesMatch>
<FilesMatch "^\.">
    Require all denied
</FilesMatch>

# Block direct access to the 'include' directory
RewriteRule ^include/ - [F,L]

# ----------------------------------------------------------------------
# | SECURITY HEADERS
# ----------------------------------------------------------------------
<IfModule mod_headers.c>
    # HSTS (Force HTTPS for 1 year)
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"

    # Clickjacking protection
    Header always append X-Frame-Options SAMEORIGIN

    # MIME-type security
    Header set X-Content-Type-Options nosniff

    # Referrer Policy
    Header set Referrer-Policy "strict-origin-when-cross-origin"

    # Permissions Policy (Disable dangerous features by default)
    Header set Permissions-Policy "geolocation=(), microphone=(), camera=()"
</IfModule>

# ----------------------------------------------------------------------
# | URL REWRITING & CANONICALIZATION
# ----------------------------------------------------------------------
RewriteEngine On

# Force WWW
RewriteCond %{HTTP_HOST} !^www\. [NC]
RewriteRule ^ https://www.%{HTTP_HOST}%{REQUEST_URI} [R=301,L]

# Force HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]

# Redirect index.php to root
RewriteCond %{THE_REQUEST} /index\.php [NC]
RewriteRule ^(.*)index\.php$ https://www.cadservicesindia.com/$1 [R=301,L]

# ----------------------------------------------------------------------
# | BOT BLOCKING (Legacy List)
# ----------------------------------------------------------------------
<IfModule mod_setenvif.c>
    SetEnvIfNoCase User-Agent ^$ bad_bot
    SetEnvIfNoCase User-Agent "Baiduspider" bad_bot
    SetEnvIfNoCase User-Agent "MJ12bot" bad_bot
    SetEnvIfNoCase User-Agent "AhrefsBot" bad_bot
    SetEnvIfNoCase User-Agent "SemrushBot" bad_bot
    SetEnvIfNoCase User-Agent "DotBot" bad_bot
    SetEnvIfNoCase User-Agent "Sogou" bad_bot
    SetEnvIfNoCase User-Agent "Exabot" bad_bot
    SetEnvIfNoCase User-Agent "BlexBot" bad_bot
    SetEnvIfNoCase User-Agent "spbot" bad_bot

    <RequireAll>
        Require all granted
        Require not env bad_bot
    </RequireAll>
</IfModule>

# ----------------------------------------------------------------------
# | COMPRESSION (GZIP)
# ----------------------------------------------------------------------
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE text/javascript
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
    AddOutputFilterByType DEFLATE application/json
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE image/svg+xml
    AddOutputFilterByType DEFLATE image/x-icon
</IfModule>

# ----------------------------------------------------------------------
# | BROWSER CACHING
# ----------------------------------------------------------------------
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresDefault "access plus 1 month"

    # HTML: No cache (ensure fresh content)
    ExpiresByType text/html "access plus 0 seconds"

    # CSS, JS, Images: Cache for 1 month
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType image/jpg "access plus 1 month"
    ExpiresByType image/jpeg "access plus 1 month"
    ExpiresByType image/gif "access plus 1 month"
    ExpiresByType image/png "access plus 1 month"
    ExpiresByType image/webp "access plus 1 month"
    ExpiresByType image/svg+xml "access plus 1 month"
    ExpiresByType image/x-icon "access plus 1 month"
</IfModule>

# Cache-Control Headers for specific files
<IfModule mod_headers.c>
    <FilesMatch "\.(js|css|jpg|jpeg|png|gif|webp|ico|svg)$">
        Header set Cache-Control "public, max-age=2592000"
    </FilesMatch>
    <FilesMatch "\.(xml|txt)$">
        Header set Cache-Control "max-age=3600, public"
    </FilesMatch>
</IfModule>

# ----------------------------------------------------------------------
# | CUSTOM REDIRECTS
# ----------------------------------------------------------------------
Redirect 301 /bim-revit-modeling-services.php https://www.cadservicesindia.com/bim-services.php
Redirect 301 /services.php https://www.cadservicesindia.com/
Redirect 301 /services/revit-family-creation-services.php https://www.cadservicesindia.com/bim-services.php
Redirect 301 /portfolio/architectural-construction-drawings-portfolio.php https://www.cadservicesindia.com/construction-documentation-sample.php
Redirect 301 /3d-architectural-modeling-services.php https://www.cadservicesindia.com/bim-services.php
Redirect 301 /2d-cad-drafting-services.php https://www.cadservicesindia.com/drafting-services.php
Redirect 301 /mep-samples.php https://www.cadservicesindia.com/mep-drafting-sample.php
Redirect 301 /3d-architectural-rendering-services.php https://www.cadservicesindia.com/3d-rendering-services.php

# ----------------------------------------------------------------------
# | ERROR PAGES
# ----------------------------------------------------------------------
ErrorDocument 404 /404.php